{
  "name": "calculator",
  "version": "1.0.0",
  "description": "A simple command line calculator",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "mocha --exit --recursive",
    "test:failures": "mocha --exit --recursive --grep 'failure'"
  }
}
